<% layout('layouts/boilerplate')%>
    
<% layout('layouts/boilerplate')%>
<div class="container text-center">
    <h1>Balloons</h1>
    
       <div class="row">
                <% for (let i = 0; i < mens.length; i++) {%>
                    <div class="col-12 col-md-3 mb-4">
                        <a class="" href="/balloons/<%= mens[i]._id  %>"><img class="mb-2 img-fluid" src="<%=mens[i].image%>" style=width:100%;height:350px;></a>
                        <h5> <%=mens[i].name%></h5>
                        <h5> <%=mens[i].description%></h5>
                        <h3>£<%=mens[i].price%></h3>
                    <form action="/basket" method="POST">
                        <input type="hidden" name="id" value="<%= mens[i]._id  %>">
                        <button type="submit" class="btn btn-primary">Add to Basket</button>
                    </form>
                    </div>
                <% } %>
        </div>
</div>

