<% layout('layouts/boilerplate')%>
    
<% layout('layouts/boilerplate')%>
<div class="container text-center">
    
    <% for (let i = 0; i < 1; i++) {%>
        <h1><%= products[i].subCategory.toUpperCase()  %></h1>
        <%}%>

       <div class="row">
                <% for (let i = 0; i < products.length; i++) {%>
                    <div class="col-12 col-md-3 mb-4">
                        <a class="" href="/fancyDress/mens/<%= products[i]._id  %>"><img class="mb-2 img-fluid" src="<%=products[i].image%>" style=width:100%;height:350px;></a>
                        <h5> <%=products[i].name%></h5>
                        <h5> <%=products[i].description%></h5>
                        <h3>£<%=products[i].price%></h3>
                    <form action="/basket" method="POST">
                        <input type="hidden" name="id" value="<%= products[i]._id  %>">
                        <button type="submit" class="btn btn-primary">Add to Basket</button>
                    </form>
                
                    </div>
                <% } %>
        </div>
</div>

