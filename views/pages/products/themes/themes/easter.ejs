<% layout('layouts/boilerplate')%>
    
<% layout('layouts/boilerplate')%>
<div class="container text-center">
    <h1>Balloons</h1>
    
       <div class="row">
                <% for (let i = 0; i < easter.length; i++) {%>
                    <div class="col-12 col-md-3 mb-4">
                        <a class="" href="/themes/easter/<%= easter[i]._id  %>"><img class="mb-2 img-fluid" src="<%=easter[i].image%>" style=width:100%;height:350px;></a>
                        <h5> <%=easter[i].name%></h5>
                        <h5> <%=easter[i].description%></h5>
                        <h3>£<%=easter[i].price%></h3>
                    <form action="/basket" method="POST">
                        <input type="hidden" name="id" value="<%= easter[i]._id  %>">
                        <button type="submit" class="btn btn-primary">Add to Basket</button>
                    </form>
                    </div>
                <% } %>
        </div>
</div>

