<% layout('layouts/boilerplate')%>
    <div class="container text-center " >
        <h1><%= name  %></h1>
        <div class="mb-3"><%- include('../../../partials/sortingDropdown') %></div>
       
    
           <div class="row ">
                    <% for (let i = 0; i < products.length; i++) {%>
                        <div class="col-12 col-md-3 mb-4">
                            <a class="" href="<%= name  %>/<%= products[i]._id  %>"><img class="mb-2 img-fluid" src="<%=products[i].image%>" style=width:100%;height:350px;></a>
                            <h5> <%=products[i].name%></h5>
                            <h5> <%=products[i].description%></h5>
                            <h3>£<%=products[i].price%></h3>
                        <form action="/basket" method="POST">
                            <input type="hidden" name="id" value="<%= products[i]._id  %>">
                            <button type="submit" class="btn btn-primary">Add to Basket</button>
                        </form>
                        </div>
                    <% } %>
            </div>
    </div>

