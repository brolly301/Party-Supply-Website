<% layout('layouts/boilerplate')%>
    <div class="container">
<div class="d-flex container my-4">
    <img class="ms-5" src="<%= product.image%>" style=width:40%;height:350px;>
   <div class="row nav justify-content-center text-center me-5">
    <h2><%= product.name%></h2>
    <h3><%= product.description%></h3>
    <h3>£<%= product.price%></h3>
    <div class="mb-5">
        <button>-</button> <span>1</span> <button>+</button>
        <div class="mt-3">
            <form action="/basket" method="POST">
                <input type="hidden" name="id" value="<%= product._id  %>">
                <button type="submit" class="btn btn-primary">Add to Basket</button>
            </form>
        </div>
    </div>
</div>
</div>


<div class="d-flex container my-4">
<div class="row nav justify-content-center text-center ">
    <form action="/balloons/<%=product._id%>/reviews" method="POST">
    <h3>Reviews</h3>
    <label for="rating">Rating</label>
    <input  style="width: 445px;" type="range" max="5" min="1" name="rating" id="rating[review]" value="<%= product._id  %>">
    <label for="review">Review</label>
    <textarea style="width: 445px;" name="body" id="body[review]" cols="30" rows="5"></textarea>
        <input type="hidden" name="id" value="<%= product._id  %>">
        <button type="submit" class="btn btn-success my-3">Post Review</button>
    </form>
 </div>
 </div>
</div>
</div>


