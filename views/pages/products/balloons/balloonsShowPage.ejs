<% layout('layouts/boilerplate')%>
    <div class="container">
<div class="d-flex container my-4">
    <img class="ms-5" src="<%= product.image%>" style=width:40%;height:350px;>
   <div class="row nav justify-content-center text-center me-5">
    <h2><%= product.name%></h2>
    <h3><%= product.description%></h3>
    <h3>£<%= product.price%></h3>
    <div class="mb-5">
        <button>-</button> <span>1</span> <button>+</button>
        <div class="mt-3">
            <form action="/basket" method="POST">
                <input type="hidden" name="id" value="<%= product._id  %>">
                <button type="submit" class="btn btn-primary">Add to Basket</button>
            </form>
        </div>
    </div>
</div>
</div>

<%- include('../../../partials/review') %>
<div>
    <h2 class="text-center">All Reviews</h2>
    <% for (let review of product.reviews) {%>
        <div class="border container-fluid mb-2">
        <h3><%= review.username  %>'s Review:</h3>
        <h4 class="mb-3"><%= review.body  %></h4>
        <h3>Rating:</h3>
        <h4><%= review.rating  %>/5</h4>

        <% if (signedInUser && signedInUser.username === review.username) { %>
        <form action="/balloons/<%= product._id  %>?_method=DELETE" method="POST">
            <button class="btn btn-danger mb-2">Delete</button>
        </form>
        <% } %>
    </div>
        <% } %>
 </div>
</div>

